import { Plugin } from '@nocobase/client';

export class PluginUserMemoryClient extends Plugin {
  async load() {
    // Settings are registered by the v2 client entry.
  }
}

export default PluginUserMemoryClient;
