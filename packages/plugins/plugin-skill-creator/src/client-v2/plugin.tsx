import { Plugin, Application } from '@nocobase/client-v2';

export class PluginSkillCreatorClient extends Plugin<Record<string, never>, Application> {
  async load() {}
}

export default PluginSkillCreatorClient;
